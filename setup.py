from setuptools import setup, find_packages

setup(name = 'fasta_reader',
    version = '1.0.0',
    packages = find_packages(),
    description = 'fasta_reader',
    python_requires = '>=3.8'
    url = 'https://github.com/skolkodetails/fasta_reader.git')